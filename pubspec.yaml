name: askar_alpha
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.1
  path_provider: ^2.1.3
  ffi: ^2.1.2
  path: ^1.9.0
  crypto: ^3.0.3
  convert: ^3.1.1
  intl: ^0.19.0
  shared_preferences: ^2.2.3
  provider: ^6.1.2

  trust_bundle_core:
    path: packages/trust_bundle_core
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
